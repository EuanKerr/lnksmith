repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: 3e8a8703264a2f4a69428a0aa4dcb512790b2c8c # v6.0.0 # v6.0.0
    hooks:
      - id: fix-byte-order-marker
      - id: check-yaml
      - id: end-of-file-fixer
      - id: trailing-whitespace
      - id: mixed-line-ending
        args: [--fix=lf]
      - id: check-added-large-files
        args: [--maxkb=500]
      - id: check-merge-conflict

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: b969e2851312ca2b24bbec879ba4954341d1bd12 # v0.15.5 # v0.15.4
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format

  - repo: local
    hooks:
      - id: ascii-check
        name: ASCII-only source files
        entry: python -c "import sys; hits=[f'{f}:{i+1}' for f in sys.argv[1:] for i,l in enumerate(open(f,'rb')) if any(b>127 for b in l)]; hits and (print('\n'.join(hits)) or sys.exit(1))"
        language: system
        types_or: [python, markdown, toml, yaml]
